import BaseCollection from 'common/collection';

/**
 * A selection of items defined by the user. similar to activeElement
 * but more explicit & controllable in JavaScript
 */

class Selection extends BaseCollection {

}

export default Selection;