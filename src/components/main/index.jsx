import React from 'react';
import CurvesComponent from '../experiments/curves';

var MainComponent = React.createClass({
  render: function() {
    return <CurvesComponent />;
  }
});

export default MainComponent;
