import BrowserApplication from './index';

describe(__filename + '#', function() {
  it('can be created', function() {
    BrowserApplication.create();
  });

  it('can be initialized', function() {
    
  });
});
