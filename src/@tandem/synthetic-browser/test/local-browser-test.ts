import { SyntheticBrowser } from "@tandem/synthetic-browser";
import { createMockBrowser } from "./helpers";


describe(__filename + "#", () => {
  let browser;

  beforeEach(() => {
    browser = createMockBrowser()
  });


});
