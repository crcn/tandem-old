#!/usr/bin/env node
var argv = require('yargs').argv;
var path = require('path');
var spawn = require('child_process').spawn;
var os    = require("os");
var createServer = require("../out/@tandem/server/server").create;

var filePath = argv._[0];

// if (filePath && !/^\//.test(filePath)) {
//   filePath = path.join(process.cwd(), filePath);
// }

// spawn('node_modules/.bin/electron', ['lib/@tandem/desktop', filePath], {
//   cwd: __dirname + "/../",
//   detached: true
// });



createServer({
  argv: argv
}).initialize();