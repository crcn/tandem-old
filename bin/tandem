#!/usr/bin/env node
var argv = require('yargs').argv;
var path = require('path');
var spawn = require('child_process').spawn;
var os    = require("os");
var createServer = require("../lib/@tandem/back-end/server").create;

var fileName = argv._[0];

// if (fileName && !/^\//.test(fileName)) {
//   fileName = path.join(process.cwd(), fileName);
// }

// spawn('node_modules/.bin/electron', ['lib/@tandem/desktop', fileName], {
//   cwd: __dirname + "/../",
//   detached: true
// });



createServer({
  argv: argv
}).initialize();