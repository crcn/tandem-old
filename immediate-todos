- re-wire history tool
  - only store diffs
- images should work
- arrow keys for changing style inputs
- fix absolute bounds
- back propagation
- get sync stop to work with vscode extension
- select synthetic objects from source