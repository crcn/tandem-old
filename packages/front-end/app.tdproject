{
  "scripts": {
    "previewServer": "/usr/local/bin/npm run tandem-preview-server",
    "build": "npm run build -- --watch"
  },
  "rootDir": ".",
  "exclude": ["node_modules"],
  "globalFilePath": "./src/global.pc"
}
