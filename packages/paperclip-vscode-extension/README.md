Paperclip is a UI language designed for live development, and compiles directly to React code. Here's an example:

```html
<span onClick="{onCounterClick}">
  Current count: {currentCount}
</span>
```
