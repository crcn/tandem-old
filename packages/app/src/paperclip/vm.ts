import { getImports } from "./dsl";
import { TreeNode } from "./tree-state";
import { Dependency, DependencyGraph } from "./loader-state";

export const evaluateModule = (module: TreeNode, graph) => {

}