Enables devs to define a color pallete to use within saffron. This would
come in the form of a tool.

```html
<color-pallete value=${[
  [['#F60', 'orange']].map(([color, label]) => { color, label })
]} />
```