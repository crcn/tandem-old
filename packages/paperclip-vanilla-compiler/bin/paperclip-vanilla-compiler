#!/usr/bin/env node
const {translatePaperclipModuleToVanilla} = require('..');
const fs = require('fs');
let filePath = process.argv[2];



const jsSource = translatePaperclipModuleToVanilla(JSON.parse(fs.readFileSync(filePath, 'utf8')));

console.log(jsSource);