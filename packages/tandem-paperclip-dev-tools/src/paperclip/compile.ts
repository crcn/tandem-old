import * as fs from "fs"; 


export const compilePaperclipFromFile = (filePath: string) => {

}

const importDependency = (cwd: string, filePath: string) => {
  
}

// export const evaluatePaperclipSource = (source: string, )